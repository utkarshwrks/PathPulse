{
  "name": "@pathpulse/sensor-sources",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//": "Unlike nav-core, this package MAY use browser APIs (geolocation, DeviceMotion, Capacitor). It hides them behind the SensorSource interface so nav-core never sees them. Implementations land in Phase 2.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "echo \"[sensor-sources] no tests yet (Phase 2)\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pathpulse/nav-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
